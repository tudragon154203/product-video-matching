name: "🧪 CI - vision-embedding (Unit)"

on:
  push:
    paths:
      - "services/vision-embedding/**"
      - "libs/**"
      - "infra/pvm/**"
      - "requirements-test.txt"
      - ".github/workflows/ci-vision-embedding.yml"
  pull_request:
    paths:
      - "services/vision-embedding/**"
      - "libs/**"
      - "infra/pvm/**"
      - "requirements-test.txt"
      - ".github/workflows/ci-vision-embedding.yml"
  workflow_call:

concurrency:
  group: vision-embedding-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit_vision_embedding:
    name: Unit/Contract | ${{ matrix.os }} | Py ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: services/vision-embedding
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04, windows-2022]
        python: ["3.10.8", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Cache pip downloads
        id: cache-pip
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~\AppData\Local\pip\Cache
          key: ${{ runner.os }}-pip-${{ matrix.python }}-${{ hashFiles('requirements-test.txt', 'services/vision-embedding/requirements.txt', '**/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-${{ matrix.python }}-
            ${{ runner.os }}-pip-

      - name: Log pip cache result
        run: |
          echo "Cache hit for pip downloads: ${{ steps.cache-pip.outputs.cache-hit }}"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r ../../requirements-test.txt

      - name: Lint
        run: flake8 .

      - name: Run unit tests
        run: pytest -q -m unit --disable-warnings --maxfail=1

      - name: Run contract tests
        run: pytest -q -m contract --disable-warnings --maxfail=1

      - name: Run integration tests
        run: pytest -q -m integration --disable-warnings --maxfail=1