# Vision Embedding Service Configuration

# Model Configuration
# Options: clip-vit-b32 (default), or any HuggingFace CLIP model ID
EMBED_MODEL=clip-vit-b32

# Device Configuration
# Options: cuda, cpu
# cuda: Use GPU for faster processing (~50-100 images/sec, requires ~1.5GB VRAM)
# cpu: Use CPU for processing (~2-5 images/sec, no VRAM usage)
DEVICE=cuda
